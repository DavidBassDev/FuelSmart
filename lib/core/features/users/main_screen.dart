import 'package:flutter/material.dart';

class MainScreen extends StatelessWidget {
  const MainScreen({super.key});

  @override
  Widget build(Object context) {
    return Scaffold(body: Text("Bienvenido admin"));
  }
}
